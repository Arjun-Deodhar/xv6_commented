/* Arjun
 * 
 * this header file has a few typedefs for
 * improving readability by replacing 
 * 	"unsigned <type>" with "u<type>"
 */
typedef unsigned int   uint;
typedef unsigned short ushort;
typedef unsigned char  uchar;

/* uint is also typedefed as pde_t, which is
 * a page directory entry
 */
typedef uint pde_t;
